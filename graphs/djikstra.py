'''
Dijkstra Algorithm

works on directed/undirected graphs
- weights must  be non-negative
- find the lengths of the shortest paths from starting vertex s to all other vertices,
and return the paths.
- Uses heap method
Runs in 0(m*log(n)) - m -> edges and n -> vertices
'''